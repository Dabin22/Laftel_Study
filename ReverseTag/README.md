#2. Reverse with tag -> HTML 태그가 포함된 문자열을 받아서, 전체 문장을 뒤집는 문제입니다. 단, 태그는 모두 그대로 적용되어야 합니다.

##풀이과정
 > 먼저 입력된 글자들이 나중에 출력되므로 스택구조를 활용합니다. 그리고 태그는 그대로이므로 큐를 사용해 저장합니다.
   스택에 저장하고 태그 부분이 나올 시점을 "tag"라는 문자열을 넣어서 그 시점에 큐를 사용합니다.

